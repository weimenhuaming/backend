syntax = "proto3";

package agent;

option go_package = "./agent";

service Agent {
  rpc Test(TestRequest) returns (TestResponse);
}

// Test 接口的请求和响应消息
message TestRequest {
  string msg = 1;
}
message TestResponse {
  string reply = 1;
}